# Skippy Environment Configuration

# Logging level (debug, info, warn, error)
LOG_LEVEL=info

# Data directory path
DATA_DIR=./data

# Embedding model (default works well for English text)
EMBEDDING_MODEL_NAME=Xenova/all-MiniLM-L6-v2

# Model cache directory (models are downloaded here on first run)
EMBEDDING_MODEL_CACHE_DIR=./models
